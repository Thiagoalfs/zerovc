port: 7880
bind_addresses:
  - ""

rtc:
  tcp_port: 7881
  port_range_start: 50000
  port_range_end: 50050
  use_external_ip: true
  node_ip: 162.35.97.76
  enable_loopback_candidate: false

# Keys are injected via environment variable LIVEKIT_KEYS in docker-compose.yml

logging:
  json: false
  level: info

room:
  auto_create: true
  empty_timeout: 300
  max_participants: 50
